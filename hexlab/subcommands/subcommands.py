from . import (
   gradient, mix,
   scheme, vision,
   similar, convert,
   adjust
)

SUBCOMMANDS = {
    'gradient': gradient,
    'mix': mix,
    'scheme': scheme,
    'vision': vision,
    'similar': similar,
    'convert': convert,
    'adjust': adjust
}
